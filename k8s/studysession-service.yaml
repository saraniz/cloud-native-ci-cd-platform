apiVersion: v1

kind: Service

metadata:
  name: study-session-service

spec:
  type: NodePort

  selector:
    app: study

  ports:
    - port: 5002
      targetPort: 5002
      nodePort: 30082

      